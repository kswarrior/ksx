# Leave empty for same-origin (when frontend+backend deployed together via backend/wrangler.toml assets)
# VITE_API_BASE=

# If you deploy backend separately on Workers, set the Worker URL:
# VITE_API_BASE=https://ksx-backend.your-subdomain.workers.dev

VITE_API_BASE=
